{% extends 'base.html.twig' %}

{% block nav_title %}Edycja pomiaru{% endblock %}

{% block body %}

    <div class="row mt-3">
        <div class="col-sm-12 mb-3">
            <div class="float-right">
                <a class="btn" href="{{ path('fitness_index') }}">
                    <span class="mdi mdi-view-list" aria-hidden="true"></span> powrót
                </a>
            </div>                 
            <div class="float-right mr-3">
                {{ include('fitness/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

    {{ include('fitness/_form.html.twig') }}

{% endblock %}
