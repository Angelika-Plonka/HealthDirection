{%  extends "base.html.twig" %}

{% block body %}

    <div>
        {% if err %}
            <h2 style="color: #FF0000;">{{ err }}</h2>
        {% endif %}

        <div class="logOut"><a href="{{ path('fos_user_security_logout') }}">Wyloguj</a></div>
        <div class="lighter">
            <h2>Witaj {{ username }}</h2>

            <h4>Wypełnij formularz, aby bla bla bla...</h4>
            {{ form(form) }}

        </div>

    </div>

{%  endblock %}